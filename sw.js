// Ein einfacher, leerer Service Worker, der nur dazu dient,
// die PWA-Installationsfähigkeit im Browser auszulösen.
self.addEventListener('fetch', (event) => {
  // Dieser Service Worker fängt keine Anfragen ab und lässt sie einfach ans Netzwerk durch.
});